//! Database schema helpers (sqlx / migrations will be used).

/// Example: return SQL to create initial tables (placeholder).
pub fn initial_sql() -> &'static str {
    "-- CREATE TABLES placeholder"
}
