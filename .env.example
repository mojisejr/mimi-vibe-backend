# Environment variables example for mimi-backend
# Copy this to .env and fill in values before running locally.
# DO NOT commit a .env file with secrets. Use your platform's secret store for production.

# Server
BIND_ADDRESS=127.0.0.1:8080    # actix bind address (overrides default)
RUST_LOG=info                  # optional, controls env_logger level

# OpenAI / LLM
OPENAI_API_KEY=                # required unless MOCK_LLM=true
OPENAI_MODEL=gpt-3.5-turbo     # optional, default used by main.rs
MOCK_LLM=false                 # set true to use mock LLM for local dev

# Database / Supabase
DATABASE_URL=postgres://user:password@localhost:5432/mimivibe
SUPABASE_URL=
SUPABASE_KEY=

# Redis / Upstash
UPSTASH_REDIS_URL=
UPSTASH_REDIS_TOKEN=

# Auth / LINE
LINE_CLIENT_ID=
LINE_CLIENT_SECRET=
# Optional LINE fields (if you implement them):
# LINE_CHANNEL_SECRET=
# LINE_CHANNEL_ACCESS_TOKEN=

# JWT / session
JWT_SECRET=change-me            # use a strong random value in production

# Payments
STRIPE_API_KEY=
STRIPE_WEBHOOK_SECRET=

# Frontend / CORS
FRONTEND_URL=http://localhost:3000

# Monitoring / misc
SENTRY_DSN=

# Notes:
# - Use `openssl rand -hex 32` to generate a secure JWT_SECRET for production.
# - Set MOCK_LLM=true and OPENAI_API_KEY=mock-api-key for offline/local testing.
